# AI와 함께 만든 풀스택 웹 서비스: "웹 복권 게임" 개발 여정

> Claude Code + Product Management 플러그인 + Superpowers 플러그인을 활용해
> 기획서(PRD) 한 장 없던 상태에서 실제 배포된 웹 서비스까지 도달한 과정 기록
>
> **결과물**: https://web-lottery-game.vercel.app · https://github.com/genej-ITC/web-lottery-game

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 목표 | 포트폴리오용 풀스택 웹 프로젝트 (로또 6/45식 번호 추첨 게임) |
| 재화 | 실제 현금/상품 없음, 가상 포인트만 사용 |
| 기술 스택 | Next.js 14 (TypeScript, App Router), Prisma, PostgreSQL(Supabase), NextAuth, Tailwind CSS |
| 배포 | GitHub → Vercel |
| 총 커밋 수 | 32개 |
| 실제 작업 시간 | 1일차 약 3시간(기획~구현~E2E 작성) + 2일차 약 2시간(최종 리뷰~배포) |

핵심 메시지: **"기획 문서 작성 → 설계 → 구현 → 테스트 → 코드리뷰 → 배포"의 전 과정을 AI와의 대화만으로 진행**했고, 그 안에서 실제 프로덕션급 버그(동시성 이슈)를 발견·수정하는 과정까지 포함된 사례.

---

## 2. 활용한 도구

### Product Management 플러그인
- 프로젝트 시작 전, **PRD(Product Requirements Document)**를 표준 템플릿으로 작성
  - Problem Statement / Goals / Non-Goals / User Stories / Requirements(P0·P1·P2 우선순위) / Success Metrics / Open Questions / Timeline Considerations
- "무엇을 만들지"와 "무엇은 만들지 않을지(Non-Goals)"를 먼저 문서로 확정한 뒤 개발에 들어감

### Superpowers 플러그인
- **Spec-Driven Development(SDD)**: PRD → 기술 설계 스펙(Design Spec) → 구현 계획(Implementation Plan) → 태스크 단위 실행
- **Test-Driven Development**: 각 태스크마다 테스트 우선 작성
- **태스크별 코드 리뷰**: 19개 태스크 각각이 구현 직후 개별 코드 리뷰를 통과
- **최종 전체 브랜치 리뷰**: 태스크별 리뷰로는 못 잡는 "교차 이슈"를 잡기 위한 마지막 통합 리뷰 단계
- **Verification Before Completion**: "됐다"고 말하기 전에 항상 실제 명령 실행 결과로 증거를 남김

---

## 3. 단계별 진행 과정

### Phase 1 — 기획: PRD 작성

Product Management 플러그인의 PRD 템플릿을 따라 다음을 문서로 확정:

- **Problem Statement**: "단순 CRUD보다 상태관리(추첨/당첨판정/포인트)와 UX가 함께 필요한 프로젝트가 포트폴리오 가치가 높다"
- **Non-Goals를 먼저 명시**: 실제 현금/결제 연동 안 함, 대규모 동시접속 최적화 안 함, 네이티브 앱 안 함, 소셜 기능 안 함 — *범위를 줄이는 결정을 먼저 문서화*
- **요구사항을 P0/P1/P2로 구분**: P0(가입·번호선택·추첨·당첨판정·히스토리·반응형)만 1차 목표로 확정, P1(연출 애니메이션·리더보드)은 "시간 남으면"으로 명시적으로 뒤로 미룸
- **Open Questions 5가지**를 문서에 남겨두고 다음 단계(설계)에서 하나씩 확정하기로 함

> 💡 **실전 팁**: PRD 단계에서 "무엇을 안 할지"를 먼저 정하는 것이, 이후 AI에게 구현을 맡길 때 범위가 무한정 커지는 것을 막는 핵심 장치였다.

### Phase 2 — 설계: Design Spec + Implementation Plan

Superpowers의 SDD 워크플로우로 PRD의 Open Questions를 전부 확정:

| PRD의 열린 질문 | 설계 단계에서 확정한 답 |
|---|---|
| 추첨 주기? | 구매 즉시 추첨 (회차 개념 없음, 즉석복권 구조) |
| 인증 방식? | 이메일 가입만 (게스트 없음) |
| 기술 스택? | Next.js 14 풀스택 |
| DB/배포? | Supabase(PostgreSQL) + Vercel |
| 포인트 밸런스? | 가입 시 10,000P, 구매 1,000P, 등수별 배당표(1000배~2배) |

이 결정들을 바탕으로 **Prisma 데이터 모델**, **핵심 트랜잭션 로직**(`purchaseTicket`), **인증 흐름**, **페이지 구성**, **테스트 전략**까지 구체적인 설계 문서로 작성한 뒤에야 코드 작성을 시작.

> 💡 **실전 팁**: "일단 코딩부터 하지 않고, 결정할 것들을 결정하고 문서로 남긴 뒤 착수"하는 순서가 이후 구현 단계를 빠르고 매끄럽게 만들었다.

### Phase 3 — 구현: 19개 태스크, TDD + 태스크별 코드 리뷰

구현 계획을 **19개의 독립적인 태스크**로 쪼개서 순서대로 진행 (스캐폴딩 → DB 스키마 → 검증 로직 → 등수 판정 → 추첨 RNG → 구매 트랜잭션 → 인증 → 서버 액션 → 각 페이지 → README):

- 태스크마다 **테스트를 먼저 작성**하고, 순수 함수(등수 판정, 번호 검증, RNG)는 DB/세션 의존성 없이 분리해 단위 테스트 가능하게 설계
- 태스크마다 구현 직후 **코드 리뷰 서브에이전트**를 붙여 검증 → 19개 태스크 전부 "review clean" 통과
- 실제 Supabase 프로젝트를 프로비저닝하고 마이그레이션까지 실행 (이 과정에서 Supabase 직접 연결 호스트가 IPv6 전용이라 접속 불가 → Connection Pooler 방식으로 전환하는 실전 트러블슈팅 발생)

결과: **25개 단위/통합 테스트 전부 통과**, `tsc` 클린, `npm run build` 성공, 실제 DB 연동까지 완료.

### Phase 4 — 최종 전체 브랜치 리뷰: Critical 버그 발견

태스크별 리뷰를 다 통과해도, **전체를 통틀어 보는 마지막 리뷰**를 별도로 진행하는 것이 Superpowers 워크플로우의 특징. 이 단계에서:

> **🔴 Critical: 구매 트랜잭션의 이중지급(Double-spend) 레이스 컨디션 발견**
>
> 포인트 차감 로직이 "잔액 확인(읽기) → 계산 → 갱신(쓰기)" 2단계로 되어 있어서, 같은 사용자가 거의 동시에 두 번 구매 요청을 보내면 두 번째 요청이 첫 번째의 차감을 덮어써서 **사실상 무료 티켓이 발급되는 위험한 취약점**이었다. 태스크별 리뷰는 "트랜잭션으로 감쌌는가"만 확인했지 "동시 요청 간 경합"까지는 보지 못했음 — 딱 전체 리뷰에서만 드러나는 교차 이슈.

**수정 방법**: DB 조건부 원자 연산(`UPDATE ... WHERE points >= cost`)으로 "확인+차감"을 하나의 원자적 쿼리로 합침. 그리고 **실제 Supabase DB에 동시 구매 요청 2개를 실제로 날려서** 정확히 1건만 성공하고 나머지는 거부되는 것을 재현·검증까지 완료.

그 외 발견/수정한 항목:
- 회원가입 비밀번호 최소 길이가 클라이언트 검증만 있고 서버 검증이 없었음 → 서버 측 검증 추가
- 구매 내역 페이지가 무제한 조회라 데이터가 쌓일수록 느려짐 → 최근 20건으로 제한
- 코드 중복(등수 라벨 맵이 두 군데 정의됨) 정리

> 💡 **실전 팁**: "각 부분은 문제없다"와 "전체가 합쳐졌을 때 문제없다"는 다른 질문이다. 동시성 버그처럼 개별 단위 리뷰로는 절대 못 잡는 유형의 결함이 있고, 이를 위해 마지막에 "교차 검증" 단계가 필요하다는 걸 실증한 사례.

### Phase 5 — E2E 테스트 환경 트러블슈팅

Playwright E2E 테스트 코드는 작성되어 있었지만, `npx playwright install chromium`이 이 개발 환경에서 **다운로드는 100% 완료되지만 압축 해제 단계에서 계속 멈추는 문제**가 있었다 (총 4회 재현, 샌드박스 해제 후에도 동일 — 진짜 환경 문제였음을 CPU 사용량 확인으로 검증).

**해결책**: Playwright 전용 번들 브라우저 다운로드를 포기하고, PC에 이미 설치된 **시스템 Chrome을 사용하도록 설정 한 줄(`channel: 'chrome'`)만 변경** → E2E 테스트 1/1 통과.

> 💡 **실전 팁**: 막힌 방법을 계속 반복하기보다, "지금 이 문제를 우회할 다른 경로가 있는가"를 먼저 검토하는 게 더 빠른 경우가 많다.

### Phase 6 — GitHub 반영

신규 GitHub 저장소(`genej-ITC/web-lottery-game`)를 원격으로 등록하고 전체 히스토리를 push. 이후 모든 수정 사항(버그 수정, 문서 갱신)을 커밋 단위로 누적.

### Phase 7 — Vercel 배포 (첫 배포 경험)

Vercel을 처음 써보는 상태에서 실제로 겪은 온보딩 과정과 막힌 지점들:

| 겪은 상황 | 실제 원인/해결 |
|---|---|
| "Import existing code vs Start from scratch" | 이미 GitHub에 코드가 있으므로 **Import existing code** 선택 |
| Vercel Team / Project Name / Root Directory 입력 | 개인 계정 선택, 프로젝트 루트에 `package.json`이 바로 있어 Root Directory는 기본값 유지 |
| "Custom Prefix" 화면 등장 | Vercel의 별도 Storage 연동(Supabase 마켓플레이스 통합) 화면이었음 — 이미 직접 만든 Supabase 프로젝트가 있으므로 **이 통합 자체를 건너뛰고** 수동으로 환경변수 등록 |
| 2FA 설정 화면 등장 | GitHub/Vercel 계정 보안 설정, 선택 사항이면 건너뛰기 가능 |
| "No Production Deployment" | 환경변수 등록 전에 흐름이 끊겨서 실제 Deploy가 트리거되지 않았던 것 — 환경변수 등록 후 빈 커밋 push로 재트리거 |
| 빌드 실패: `TypeError: Invalid URL, input: ''` | **`NEXTAUTH_URL` 환경변수 값이 빈 문자열로 저장됨** — NextAuth가 빌드 시점에 URL을 파싱하다 실패. 값 재확인 후 재배포로 해결 |
| 배포 성공 후 실제 도메인 확인 | `web-lottery-game.vercel.app` — 사전에 추측했던 값과 정확히 일치 |

배포 성공 후, **실제 프로덕션 URL에서 회원가입 → 구매 2회 → 로그아웃 → 로그인 → 구매내역 확인**까지 수동 골든패스 검증 완료.

> 💡 **실전 팁**: 처음 해보는 배포 과정에서 마주치는 막힘은 대부분 "아직 안 끝난 설정" 또는 "빈 값/오타" 같은 사소한 원인이다. 에러 메시지(`Invalid URL, input: ''`)를 그대로 읽으면 원인을 좁힐 수 있다.

### Phase 8 — 배포 후 운영 질문들

배포가 끝난 뒤에도 실제 서비스 운영자로서 자연스럽게 따라온 질문들:

- **실제 DB 데이터를 어떻게 보나?** → Supabase 대시보드의 Table Editor(가장 쉬움) 또는 `npx prisma studio`(로컬 GUI)로 확인 가능
- **배포한 URL은 정말 전 세계에 공개된 건가?** → 그렇다. `/results`는 원래 공개 설계였지만 `/signup`, `/login`도 공개라 누구나 가입·구매까지 가능한 상태임을 확인
- **무료 플랜 한도를 넘으면 자동으로 카드 결제가 되나?** → 아니다. Supabase/Vercel 모두 사용자가 **직접 유료 플랜에 카드를 등록**해야 과금이 시작되며, 무료 한도 초과 시엔 자동 청구가 아니라 서비스 제한(요청 차단, 프로젝트 일시정지 등)이 걸리는 구조

---

## 4. 전체 타임라인 요약

```
1일차 (2026-09-05)
13:35  PRD → 설계 스펙 → 구현 계획 확정
14:04  프로젝트 스캐폴딩 시작
14:04~14:48  19개 태스크 중 핵심 로직/페이지 구현 (약 45분, 태스크별 리뷰 포함)
15:16  Supabase 프로비저닝 + 마이그레이션 (Pooler 전환 트러블슈팅)
15:19~15:26  히스토리/결과 페이지, 렌더링 버그 수정
16:32  E2E 테스트 코드 작성
16:33  세션 종료, PROGRESS.md로 인수인계 기록

2일차 (2026-09-07)
08:25  문서 보완 (DIRECT_URL 누락 발견)
08:2x  최종 전체 브랜치 리뷰 실행 → Critical 버그(이중지급 레이스) 발견
08:36  Critical 버그 수정 + 실제 DB 동시성 재현 테스트로 검증
08:53  E2E 테스트 환경 문제 해결 (시스템 Chrome 채널로 우회) → 실행 성공
10:06  GitHub 저장소 반영, Vercel 최초 배포 시도
10:26  환경변수 문제(NEXTAUTH_URL 빈 값) 해결 후 재배포
10:33  프로덕션 배포 완료, 실제 골든패스 수동 검증 완료
```

**PRD 작성 시작부터 실제 서비스 배포까지, 실질 작업 시간 약 5시간.**

---

## 5. 이 사례가 보여주는 것 (교육적 시사점)

1. **기획(PRD) 없이 바로 코딩하지 않았다** — Non-Goals와 P0/P1/P2 우선순위를 먼저 문서화한 것이, 이후 AI가 범위를 벗어나지 않고 구현하게 만든 핵심 장치였다.
2. **설계 문서가 구현 속도를 만든다** — Open Questions를 설계 단계에서 전부 확정한 뒤 코딩을 시작하니, 구현 중 왔다갔다하는 의사결정 지연이 없었다.
3. **작은 단위 + 즉시 리뷰의 반복**이 큰 버그를 줄이지만, **전체를 보는 마지막 리뷰**가 별도로 필요하다 — 이번 프로젝트의 유일한 Critical 버그(동시성 이중지급)는 개별 태스크 리뷰가 아니라 전체 브랜치 리뷰에서만 잡혔다.
4. **"됐다"는 주장 전에 항상 실행 증거를 남긴다** — 테스트 통과 수치, 빌드 로그, 실제 DB 쿼리 결과, 실제 프로덕션에서의 수동 QA까지, 매 단계마다 "말"이 아니라 "증거"로 검증했다.
5. **막히면 우회로를 찾는다** — Playwright 설치가 막혔을 때 계속 재시도만 하지 않고, 시스템 브라우저를 쓰는 대안으로 전환해 문제를 해결했다.
6. **배포는 개발의 끝이 아니라 새로운 질문의 시작** — 배포 직후에도 "누가 볼 수 있나", "비용은 어떻게 되나", "데이터는 어떻게 보나" 같은 운영 질문이 자연스럽게 이어졌고, 이것도 개발 과정의 일부다.

---

## 6. 부록: PPT 슬라이드 구성 제안

이 문서를 PPT로 옮길 때 아래 순서를 참고하면 좋습니다 (섹션 = 슬라이드 그룹):

1. **타이틀** — 프로젝트명, 배포 URL, 한 줄 요약
2. **프로젝트 개요** (2절 표 활용)
3. **활용 도구 소개** — Product Management 플러그인 / Superpowers 플러그인 (3절)
4. **Phase 1~2: 기획과 설계** — PRD Non-Goals/P0 우선순위, Open Questions 확정 표
5. **Phase 3: 19개 태스크 TDD 구현** — 순수 함수 분리, 태스크별 리뷰 개념
6. **Phase 4: Critical 버그 발견 (하이라이트 슬라이드)** — 이중지급 레이스 컨디션, 발견 원리(전체 리뷰), 수정/검증 방법 — 가장 임팩트 있는 슬라이드
7. **Phase 5: 환경 트러블슈팅** — Playwright 우회 사례
8. **Phase 7: Vercel 첫 배포 실전기** — 겪은 막힘 표(7절 표) 그대로 활용, 초심자 공감 포인트
9. **타임라인 요약** — 4절의 타임라인을 인포그래픽으로
10. **교육적 시사점 6가지** — 클로징 슬라이드, 각 항목 1장씩 또는 한 슬라이드에 리스트
